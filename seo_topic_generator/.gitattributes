# =====================================
# Git Attributes for SEO Topic Generator
# =====================================

# --- Ensure consistent text formatting ---

# Normalize all text files to LF endings in repo, auto-convert locally
* text=auto eol=lf

# Force encoding to UTF-8 for critical files
*.py text working-tree-encoding=UTF-8
*.md text working-tree-encoding=UTF-8
*.json text working-tree-encoding=UTF-8
*.csv text working-tree-encoding=UTF-8
*.txt text working-tree-encoding=UTF-8

# --- Handle binary files correctly ---

# Images (if you later add assets)
*.png binary
*.jpg binary
*.jpeg binary
*.gif binary
*.ico binary

# Fonts (optional for future if you have custom outputs)
*.woff binary
*.woff2 binary
*.ttf binary

# --- Special cases ---

# Treat Jupyter notebooks as binary (optional: if you add experiments later)
*.ipynb binary
